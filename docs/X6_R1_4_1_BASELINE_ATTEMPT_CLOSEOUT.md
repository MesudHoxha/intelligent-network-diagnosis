# X6-R1.4.1 — Baseline Attempt Closeout Candidate

Status: `PENDING_SOURCE_ONLY_CLOSEOUT_REVIEW`.

The published R1.4.1 lifecycle at
`7fd529c101503dfebf555825db0dbc4d517ecefb` consumed the single authorization
for `x6-r1-4-1-baseline-only-20260919-a01`, collected all C01–C20 and H01–H10
windows, froze the C01–C10 threshold manifest before C11, cleaned its owned
resources, and completed independent replay. Its durable terminal remains
`SOURCE_CONTRACT_COMPLETE_REPLAY_VERIFIED` with `qualified:false`. That result
establishes lifecycle, evidence, cleanup, and replay integrity; it does not
itself express baseline qualification.

A later read-only assessment outside the repository and original run tree used
the accepted R1.3.1 evaluator. All 100 C11–C20 and H01–H10 numeric comparisons
passed the frozen inclusive boundaries after six-place half-even normalization.
Every window also had direct rate-limit exclusion evidence from exact `noqueue`
handle `0:` state and empty root and ingress filters. The resulting external
baseline assessment is `QUALIFIED`. The original terminal is unchanged, and
this assessment does not establish scientific acceptance, Evidence v4, Feature
Vector v2, diagnosis, dataset, model, metric, API, thesis, or generalized
scientific results.

The frozen manifest carries embedded content SHA-256
`0db20fccbb22f7bcde7bc5d7be0d3fd8923e32d663d59297c178dd300ce7c301`;
the complete canonical manifest file, including that member, has SHA-256
`40e71ce6275412adae4b956a050c116862e4f0c987ee556e67a52d5a0ae84eeb`.
The qualification assessment, evaluator input, input inventory, and evidence
manifest are bound by the companion versioned closeout record.

The original production CLI top-level return code was not preserved. Replay
return code zero does not replace it. This remains a documented evidence
limitation; the accepted materialized verifier instead requires the bounded raw
command return codes, all of which remain in the immutable run evidence.

The accepted expansion sequence identifies
`PROSPECTIVE_SUCCESSOR_PARAMETER_THRESHOLD_FREEZE` as the next step. It has not
started and is not authorized by this closeout candidate. It requires explicit
acceptance of this closeout, fresh verification of the bound hashes, and a
separately reviewed source-only freeze proposal that preserves the accepted
threshold mathematics and claim boundaries. Future F1 justification or runtime
work remains subject to the later
`SEPARATE_REVIEW_OF_FUTURE_F1_JUSTIFICATION` stage and fresh explicit
authorization.

Historical authorization remains `0/10_FALSE`. X5 authority, consumed-pilot
evidence, all original runtime and assessment evidence, and all four protected
stashes remain unchanged.
