# HANDOFF X4-R0 — DHCP, DNS, and Service Security Design Gate

Status: ACCEPTED DESIGN ONLY — X4-R1 IS NEXT

## Completed

- append-only X4-R0 plan, schema, source-only verifier and contract tests;
- hash binding to accepted X0/X1 and X3-R5 closeout boundaries;
- six-node service topology context with separate DHCP, DNS and application
  service flows;
- nine-feature ownership split across the two existing X1 design-only
  collectors;
- five exact disjoint single-fault signatures and seven-release sequence; and
- ten false runtime/scientific authorization flags.

## Boundary

X4-R0 is not a Containerlab release. It creates no topology deployment,
network mutation, Evidence v4, prediction, dataset, ML/Hybrid output, metric,
API change or multiple-fault claim. Active probes and generic connectivity are
future effectiveness controls, not root-cause classifiers.

Frozen Phase 6–9, API v1, accepted X2/X3 sources/evidence and P9-R1 remain
unchanged. Each later X4 runtime slice must receive separate authorization and
prove durable recovery, baseline restoration and zero-container cleanup.

## Next release

`X4_R1_DHCP_SERVER_UNAVAILABLE` only.
