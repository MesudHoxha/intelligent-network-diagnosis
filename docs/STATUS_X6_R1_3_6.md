# X6-R1.3.6 status

X6-R1.3.6 is a source-only correction to the production-path gaps identified
by the R1.4 review. It adds a future-R1.4-only production CLI, real timing
adapter, canonical 30-window lifecycle, recovery prevalidation CLI, and
independent materialized reconstruction. It creates no R1.4 authorization and
does not execute runtime commands. The ten historical authorization fields
remain false; the highest synthetic verifier outcome is source-contract
completion for later authorization review. X6-R1.4 remains paused.

The frozen R1.3.5 gate is evaluated in the exact published
`03af67568c9ee55398d2bf7f8d4f76091d7f73b5` snapshot. Its shared decision and
status documents remain byte-identical in this successor worktree; R1.3.6
uses these versioned records instead.
