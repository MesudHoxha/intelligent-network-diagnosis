# X6-R1.3.9 handoff

The active successor CLI is
`python -m src.orchestration.x6_r1_3_9_production_path`; standalone recovery is
`python -m src.orchestration.x6_r1_3_9_recovery`. Both require a separately
bound future authorization, and neither is authorized by this source release.

Review cleanup reconstruction through the raw `cleanup_before`, optional
owned-topology cleanup action, and `cleanup_after` command groups. Review
server absence through the paired container and exact-process probes; no
`server_stop` return code 1 is accepted as success.

After focused verification and predecessor-context preflight, freeze a final
source inventory before running the still-required complete primary and
archive-free source-only suites. Preserve precisely the six accepted historical
diagnostics. R1.4 remains paused.
