# X6-R0.1 measurement and traffic methodology gate

X6-R0.1 is source-only. It freezes the future five-node traffic context,
commands, tool-version capture, warm-up and window counts, exact RTT p95
calculation, threshold-manifest rule, raw numeric collector boundary, and
separate mutation-effectiveness measurements. It creates no performance
runtime evidence, threshold value, metric, dataset, model, API change, or
claim.

F1--F4 vectors remain conditional until their future empirical pilot windows
truthfully demonstrate separation. F3 requires a finite-capacity bottleneck
with queue/counter ownership; F4 requires both direct shaping configuration and
throughput consistent with a manifest-declared tolerance.

X5-R9 remains next. X6-R1 and P9-R2 remain paused.
